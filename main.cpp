#include "common.h"

int main() 
{
    // tests ();

    // draw (GetPoint);

    

    // for (int i = 0; i < 101; i++)
    // {
    //     printf ("%d %d %d\n", array_color[i].red, array_color[i].green, array_color[i].blue);
    // }

    // draw (GetPoint2);
    tests ();


    return 0;
}
